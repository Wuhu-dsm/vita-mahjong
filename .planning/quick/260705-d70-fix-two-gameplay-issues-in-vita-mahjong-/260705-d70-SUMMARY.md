---
status: complete
---

# Quick Task 260705-d70: Fix rapid-tap drops and rectangular layouts

**Completed:** 2026-07-05

## Changes Made

1. **Allowed rapid taps to execute directly in `src/renderer/screens/GameScreen.ts`**
   - Removed the global `inputLocked` discard behavior and the tap queue.
   - `handleTileTap` now processes every tap immediately, even while other tiles are flying.
   - Added `activeAnimations` and `flyingStones` tracking so the board re-render is deferred until all in-flight/match-removal animations finish, and the tray only shows tiles that have actually arrived.
   - Assist buttons (undo/hint/shuffle) are blocked while animations are running to avoid state conflicts.
   - The board is re-rendered automatically once the last animation ends.

2. **Made level layouts less regular in `src/generator/LayoutBuilder.ts`**
   - Added deterministic row-width variation to `layerPositions` via `distributeRowWidths`.
   - When a layer would be a perfect rectangle, an extra row is added so the shape gains jagged edges while keeping the same tile count.
   - Width reductions are distributed deterministically per level/layer using a seeded hash, keeping layouts reproducible.

3. **Regenerated level data and verified**
   - Ran `npm run generate-levels` to rebuild `public/levels/*.json`.
   - Ran `npm run build` to update `dist/levels/*.json` and confirm the production build succeeds.
   - Ran `npm test` — all 58 tests pass.

## Verification

- `npm test` passes (58/58).
- `npm run build` succeeds.
- Generated levels remain solvable and within the original tile-count budgets.
