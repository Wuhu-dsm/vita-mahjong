# Quick Task 260705-d70: Fix rapid-tap drops and rectangular layouts

**Date:** 2026-07-05
**Mode:** quick (agents unavailable — executed inline)

## Goal

Fix two gameplay issues reported by the user:

1. **Rapid tile taps are ignored** while the flight/match-removal animations are playing.
2. **Generated levels look too regular** — perfect rectangles — and would look better with some irregular gaps and row-width variation while remaining solvable.

## Tasks

### Task 1: Queue rapid taps during animation

- **File:** `src/renderer/screens/GameScreen.ts`
- **Action:** Replace the global `inputLocked` discard behavior with a per-tap queue. When a tap arrives while an animation is running, enqueue the stone id and process it after the current animation sequence finishes. Keep the lock only for the duration of the state update + animation so multiple rapid taps are handled sequentially instead of dropped.
- **Verify:** Unit/integration tests still pass; manual play shows rapid consecutive taps are accepted.

### Task 2: Make level layouts less regular

- **File:** `src/generator/LayoutBuilder.ts`
- **Action:** Introduce row-width variation and small controlled gaps in `layerPositions`. Keep each layer count even and the total tile count unchanged so existing budget tests continue to pass. Ensure upper-layer tiles retain support from the layer below by not removing tiles that are the only support for an upper tile.
- **Verify:** `generator.test.ts` passes; `generate-levels` script produces solvable JSON files for all 20 levels.

### Task 3: Regenerate level assets and run tests

- **Files:** `public/levels/*.json`, `dist/levels/*.json`
- **Action:** Run `npm run generate-levels` to rebuild level data with the new layouts. Run `npm test` to confirm nothing is broken.
- **Verify:** All tests pass and levels remain solvable.

## must_haves

- Rapid taps during animations are queued and executed, not dropped.
- Level layouts are visibly less rectangular but still solvable and pass all existing tests.
- `npm test` passes.
- Level JSON files are regenerated.
