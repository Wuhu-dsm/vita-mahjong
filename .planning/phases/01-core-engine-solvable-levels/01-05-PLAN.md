---
phase: 01-core-engine-solvable-levels
plan: 05
type: execute
wave: 5
depends_on:
  - 04
files_modified:
  - src/renderer/screens/GameScreen.ts
  - src/renderer/components/TileSprite.ts
  - src/renderer/components/BlockedHint.ts
  - src/app/config.ts
  - src/__tests__/blocked-feedback.test.ts
autonomous: true
gap_closure: true
requirements:
  - ACCS-03
must_haves:
  truths:
    - "Blocked tile taps never move or select the tile."
    - "Blocked feedback clears automatically after the temporary hint window."
    - "Blocked feedback shows one coherent red-arrow/text cue without obscuring adjacent tiles."
  artifacts:
    - path: "src/renderer/screens/GameScreen.ts"
      provides: "Timed lifecycle for blocked feedback and cleanup on next tap/level reset."
    - path: "src/renderer/components/TileSprite.ts"
      provides: "Tile dimming state without persistent external arrows."
    - path: "src/renderer/components/BlockedHint.ts"
      provides: "Temporary compact red-arrow/text hint."
    - path: "src/__tests__/blocked-feedback.test.ts"
      provides: "Regression coverage for blocked feedback cleanup and non-duplicated arrows."
  key_links:
    - from: "src/renderer/screens/GameScreen.ts"
      to: "src/renderer/components/TileSprite.ts"
      via: "blocked tap calls tile feedback APIs and later clears them"
      pattern: "setBlocked"
    - from: "src/renderer/screens/GameScreen.ts"
      to: "src/renderer/components/BlockedHint.ts"
      via: "blocked tap positions the temporary hint near the tapped tile"
      pattern: "showAt"
---

<objective>
Close the Phase 01 UAT gap for blocked-tile feedback. The engine already prevents blocked tile selection; this plan fixes the rendered feedback so the abnormal dim/arrow state clears promptly and arrows do not obscure neighboring tiles.

Purpose: Senior-friendly touch feedback must be clear, temporary, and readable on a dense mobile Mahjong board.
Output: A verified blocked-feedback lifecycle and visual cue ready for re-running UAT test 3.
</objective>

<execution_context>
@/Users/mac/.codex/gsd-core/workflows/execute-plan.md
@/Users/mac/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-engine-solvable-levels/01-CONTEXT.md
@.planning/phases/01-core-engine-solvable-levels/01-UI-SPEC.md
@.planning/phases/01-core-engine-solvable-levels/01-04-SUMMARY.md
@.planning/phases/01-core-engine-solvable-levels/01-UAT.md
@.planning/debug/blocked-tile-feedback.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a timed blocked-feedback lifecycle</name>
  <files>src/renderer/screens/GameScreen.ts, src/renderer/components/TileSprite.ts, src/app/config.ts, src/__tests__/blocked-feedback.test.ts</files>
  <read_first>
    - 01-UAT.md gap for test 3.
    - .planning/debug/blocked-tile-feedback.md evidence and root cause.
    - GameScreen.handleTileTap blocked branch.
    - TileSprite.setBlocked implementation.
  </read_first>
  <action>
    Add a single configurable blocked feedback duration, e.g. `config.timings.blockedFeedbackMs = 1200`, and stop hardcoding the duration in components. Update `GameScreen` so the blocked-tap path clears any previous blocked tile feedback before applying feedback to the newly tapped tile. Track the current blocked `TileSprite` and an expiry timestamp; in the existing ticker callback, clear that tile with `setBlocked(false)` once the duration expires. Also clear blocked feedback when a level is loaded, when the board is re-rendered after a successful move, and before showing feedback for another blocked tile. Keep the gameplay rule path unchanged: blocked tiles must still return without entering the tray or being removed.

    Add regression coverage that proves the lifecycle contract. Prefer a small unit around an extracted helper or mocked `GameScreen` feedback method so the test can run in the existing Vitest node environment without a real canvas. The test should cover: previous blocked tile is cleared when another blocked tile is tapped, the current blocked tile clears after the feedback duration, and successful board re-render/reset clears stale feedback.
  </action>
  <acceptance_criteria>
    - A blocked tile's dimmed state clears automatically after the configured feedback duration.
    - Tapping a different blocked tile clears feedback on the previous tile first.
    - Starting/restarting/re-rendering a level cannot leave stale blocked feedback behind.
    - No change to BoardModel/GameState free-tile legality.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run src/__tests__/blocked-feedback.test.ts src/__tests__/GameState.test.ts</automated>
  </verify>
  <done>Blocked tile dimming has a deterministic lifecycle and cannot persist indefinitely.</done>
</task>

<task type="auto">
  <name>Task 2: Remove duplicate external arrows and keep the hint compact</name>
  <files>src/renderer/components/TileSprite.ts, src/renderer/components/BlockedHint.ts, src/renderer/screens/GameScreen.ts, src/__tests__/blocked-feedback.test.ts</files>
  <read_first>
    - TileSprite arrow creation and setBlocked behavior.
    - BlockedHint arrow/text layout.
    - UAT report that neighboring tiles are obscured by arrows.
  </read_first>
  <action>
    Make `BlockedHint` the single source of red-arrow + "被左右锁住" messaging. Change `TileSprite.setBlocked(true)` so the tile itself only dims/tints and does not show persistent left/right arrows outside the tile face. Either remove the tile-local arrow sprites or keep them permanently hidden if removing them causes unnecessary churn. Then tighten `BlockedHint` so its arrows/text stay visually grouped around the tapped tile: reduce arrow size/offset as needed, preserve readable text, and avoid z-order or coordinate changes that cover the tray/HUD. If clamping is needed, do it in `GameScreen` when calling `blockedHint.showAt(...)` so the hint stays inside the board/canvas safe area.

    Extend the blocked-feedback test or add a static/component-level assertion that `TileSprite` no longer exposes active persistent arrow visibility for blocked state. Keep one manual UAT checkpoint in the SUMMARY: tap a blocked tile in a 20:9 portrait viewport and confirm the cue fades and adjacent tiles remain readable.
  </action>
  <acceptance_criteria>
    - A blocked tap shows one coherent cue: temporary hint arrows/text plus short tile dimming.
    - Tile-local arrows no longer remain visible after the hint fades.
    - The hint does not obscure neighboring tile faces, tray slots, or HUD controls in the mobile portrait layout.
    - The Chinese text "被左右锁住" remains readable.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run src/__tests__/blocked-feedback.test.ts && npm run build</automated>
    <manual>Re-run Phase 01 UAT test 3 on a 20:9 portrait viewport.</manual>
  </verify>
  <done>Blocked feedback is temporary, non-duplicated, and visually readable around adjacent tiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User touch input -> GameScreen | Pointer events trigger visual feedback and game-state checks. |
| GameScreen -> TileSprite/BlockedHint | Renderer state controls temporary feedback visibility. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05-01 | Denial of Service | GameScreen feedback lifecycle | low | mitigate | Reuse the existing ticker; keep at most one active blocked-feedback target and clear it on reset/re-render. |
| T-01-05-02 | Tampering | Gameplay rule path | low | mitigate | Do not alter BoardModel/GameState blocked legality; include GameState regression test in verification. |
| T-01-05-03 | Usability/Safety | Visual feedback | medium | mitigate | Remove duplicate persistent arrows and require mobile UAT for readability before closing the gap. |
</threat_model>

<verification>
- `npm test -- --run src/__tests__/blocked-feedback.test.ts src/__tests__/GameState.test.ts`
- `npm run build`
- Re-run Phase 01 UAT test 3: blocked tile feedback fades and arrows do not obscure adjacent tiles.
</verification>

<success_criteria>
- Blocked tile taps remain non-selecting and non-removing.
- Tile dimming clears after the feedback duration.
- Red arrows/text appear as a single temporary hint rather than persistent duplicate arrows.
- Nearby tile faces remain readable in a 20:9 portrait mobile viewport.
- The gap in `01-UAT.md` can be re-tested and marked resolved after implementation.
</success_criteria>

<output>
Create `.planning/phases/01-core-engine-solvable-levels/01-05-SUMMARY.md` when done, including the manual UAT checkpoint for blocked-tile feedback.
</output>

## Artifacts this phase produces

| Symbol | File | Kind | Consumers |
|--------|------|------|-----------|
| Blocked feedback lifecycle | `src/renderer/screens/GameScreen.ts` | renderer state | blocked tap handling |
| Tile dim-only blocked state | `src/renderer/components/TileSprite.ts` | component behavior | GameScreen |
| Compact blocked hint | `src/renderer/components/BlockedHint.ts` | component layout | GameScreen |
| Blocked feedback regression test | `src/__tests__/blocked-feedback.test.ts` | test | CI, verification |
