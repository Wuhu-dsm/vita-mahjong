---
phase: 02-ui-ux-accessibility-game-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/ScreenManager.ts
  - src/renderer/screens/HomeScreen.ts
  - src/renderer/screens/GameScreen.ts
  - src/renderer/components/HUD.ts
  - src/app/App.ts
  - src/renderer/screens/ResultScreen.ts
autonomous: true
requirements:
  - CORE-06
  - LVLS-05
  - ACCS-01
  - ACCS-02
  - "ACCS-05 (REMOVED per D-03 — no low-pressure mode)"
  - "VISL-01 (verify existing Phase 1 implementation)"
  - "VISL-02 (verify existing Phase 1 implementation)"
  - "RSLT-01 (verify existing Phase 1 implementation)"
  - "RSLT-02 (verify existing Phase 1 implementation)"
  - "PLAT-01 (verify existing Phase 1 implementation)"

must_haves:
  truths:
    - "TRUTH-1: User taps level button showing current level number to start game (linear-only, D-01/D-02)"
    - "TRUTH-2: User taps HUD back button to return home immediately — no confirmation, progress discarded (D-11)"
    - "TRUTH-3: Tiles on same layer abut edge-to-edge (gridX=113.5, gridY=120); upper layers shift by layerOffset=28 (D-08/D-09)"
    - "TRUTH-4: Timer MM:SS is visible in HUD between score and combo; stops at win/deadlock"
    - "TRUTH-5: Completing a level advances currentLevel; home button reflects new number"
  artifacts:
    - ScreenManager.ts with settings ScreenName and Container
    - HomeScreen.ts with dynamic levelButton.setLabel() and gear icon pointertap event
    - HUD.ts with back button callback and timerLabel
    - GameScreen.ts with updated toBoardPosition() grid parameters
    - App.ts with currentLevel tracking
  key_links:
    - HomeScreen gear click → ScreenManager.show('settings')
    - HUD back click → ScreenManager.show('home') + GameState discard
    - GameScreen.WIN → currentLevel increment → HomeScreen.setLevel()
---

<objective>
Extend Phase 1's three-screen architecture with a settings screen slot, wire the navigation flow (home to game to result, game to home discard, home to settings), implement dynamic linear level progression per D-01/D-02, update tile grid positioning per D-08/D-09, add a HUD timer, and activate the HUD back button.

Purpose: Foundation for all Phase 2 UI work — every subsequent plan needs the settings screen slot, level tracking state, and grid/timer infrastructure.
Output: Working 4-screen architecture with correct tile layout, dynamic level button, and timer display.
</objective>

<execution_context>
@/Users/mac/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/mac/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-ui-ux-accessibility-game-flow/02-CONTEXT.md
@.planning/phases/02-ui-ux-accessibility-game-flow/02-RESEARCH.md
@.planning/phases/02-ui-ux-accessibility-game-flow/02-UI-SPEC.md
@src/renderer/ScreenManager.ts
@src/renderer/screens/HomeScreen.ts
@src/renderer/screens/GameScreen.ts
@src/renderer/components/HUD.ts
@src/app/App.ts
@src/app/config.ts
@src/renderer/screens/ResultScreen.ts
@src/renderer/components/Button.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend ScreenManager with Settings + HomeScreen dynamic level button</name>
  <files>src/renderer/ScreenManager.ts, src/renderer/screens/HomeScreen.ts</files>
  <read_first>
Read these files before editing — must understand current state before modifying:
- src/renderer/ScreenManager.ts — current ScreenName union, screens record, transitionScale record, show(), resizeToRenderer(), applyScale()
- src/renderer/screens/HomeScreen.ts — how levelButton is created, gear icon positioning, START_LEVEL event emission pattern
- src/renderer/components/Button.ts — ButtonOptions interface, setLabel() method, requireTexture pattern
- src/app/config.ts — designWidth, designHeight, safeAreaTop, colors
  </read_first>
  <action>
ScreenManager.ts changes per D-01, D-02:
1. Extend ScreenName union type: add | 'settings' so the type is 'home' | 'game' | 'result' | 'settings'.
2. Add readonly Container property: readonly settings = new Container(); alongside existing home, game, result.
3. Add settings: this.settings entry to the screens record literal.
4. Add settings: 1 entry to the transitionScale record literal.
5. Verify show() already uses this.screens[name] generically — if resizeToRenderer() or applyScale() iterates Object.keys(this.screens), the new entry is automatically included. If it hardcodes screen names, add settings to that loop.

HomeScreen.ts changes per D-01, D-02, D-14:
1. Store levelButton as a private property named levelButton: Button. In the constructor, assign this.levelButton = new Button(...) instead of a local variable.
2. Add public method setLevel(n: number): void that calls this.levelButton.setLabel(template literal: 关卡 ${n}).
3. Add new static event string static readonly OPEN_SETTINGS = 'open-settings'.
4. Wire gear icon: currently the gear Sprite at (designWidth*0.93, designHeight*0.06) has eventMode='static', cursor='pointer', and a hitArea but NO onTap handler. Add gearSprite.on('pointertap', () => this.emit(HomeScreen.OPEN_SETTINGS));.
5. Change the START_LEVEL event emission: remove the hardcoded level argument — emit as this.emit(HomeScreen.START_LEVEL) with no payload (App.ts will read currentLevel directly). The button's onTap callback in the Button constructor should call: () => this.emit(HomeScreen.START_LEVEL).
  </action>
  <acceptance_criteria>
- ScreenName type union includes 'settings' — verify with grep that string appears 3+ times in the file
- ScreenManager has readonly settings property typed as Container
- screens record has settings: this.settings entry
- transitionScale record has settings: 1 entry
- HomeScreen has public method setLevel(n: number)
- HomeScreen has static OPEN_SETTINGS = 'open-settings'
- Gear icon pointertap callback emits HomeScreen.OPEN_SETTINGS
- START_LEVEL emit has no numeric argument (no payload)
- npm run build succeeds with no TypeScript errors
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>ScreenManager accepts 'settings' as a valid screen name. HomeScreen button label is dynamic via setLevel(). Gear icon click emits OPEN_SETTINGS event. npm run build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Update tile grid parameters + HUD back button + timer display</name>
  <files>src/renderer/screens/GameScreen.ts, src/renderer/components/HUD.ts</files>
  <read_first>
Read these files before editing:
- src/renderer/screens/GameScreen.ts — toBoardPosition() current gridX=64 gridY=78 layerOffset=18, ticker loop, startLevel(), loadLevelModel(), finishTapResult(), getStats(), WIN emission
- src/renderer/components/HUD.ts — constructor, update(level, score, combo), back button Container, comboLabel position at designWidth*0.73, createIconButton()
- src/app/config.ts — designWidth, designHeight, safeAreaTop, hud.height=96
- .planning/phases/02-ui-ux-accessibility-game-flow/02-UI-SPEC.md — HUD layout table (x positions), tile layout parameter table
  </read_first>
  <action>
GameScreen.ts changes per D-08, D-09, D-10:
1. Update toBoardPosition(stone: Stone) grid parameters:
   - gridX = 113.5 (was 64) — tile.width / 2 = 227/2, ensures same-layer edge-to-edge abutment (D-08)
   - gridY = 120 (was 78) — tile.height, ensures same-layer edge-to-edge abutment (D-08)
   - layerOffset = 28 (was 18) — tile.width / 8, creates classic upper-tile-covers-4-lower-tiles effect (D-09)
   - The formula remains: x = stone.x * gridX + stone.z * layerOffset; y = stone.y * gridY - stone.z * layerOffset
2. In startLevel(): add this.startedAt = Date.now() before returning. Add private startedAt: number = 0 field to the class.
3. In the ticker callback (where animations are processed): add timer update logic — if this.state exists and the game is not terminal, compute elapsedMs = Date.now() - this.startedAt, format as MM:SS, call this.hud.update(this.state.getLevel().id, this.state.getScore(), this.state.getCombo(), elapsedMs).
4. Check GameState for a terminal field — if accessible, use it. If terminal is private, add a public isTerminal(): boolean method to GameState that returns the terminal flag.
5. In getStats(): ensure elapsedSeconds is computed from Date.now() - this.startedAt (rounded to whole seconds via Math.floor).

HUD.ts changes per D-11:
1. Add optional callback to constructor: constructor(onBack?: () => void). Store as private onBack?: () => void.
2. In createIconButton('icon_back', ...): after creating the Container with eventMode='static' and cursor='pointer', add container.on('pointertap', () => { if (this.onBack) this.onBack(); });. The back button uses btn_circle_brown texture (76px), icon 42px wide — preserve this layout.
3. Add new private timerLabel: Text — create in constructor at position (designWidth * 0.615, safeAreaTop + 46), anchor 0.5. TextStyle: fontSize: 30, fontWeight: '800', fontFamily: 'Vita Noto Sans SC, Noto Sans SC, PingFang SC, Microsoft YaHei, sans-serif', fill: 0xfff4d6, stroke: 0x08352c, strokeThickness: 4. Initial text: '00:00'.
4. Shift comboLabel x position from designWidth * 0.73 to designWidth * 0.78.
5. Extend update() method signature: update(level: number, score: number, combo: number, timerMs?: number): void. When timerMs is provided, format as MM:SS using Math.floor(timerMs / 60000).toString().padStart(2, '0') + ':' + Math.floor((timerMs % 60000) / 1000).toString().padStart(2, '0') and set this.timerLabel.text.
6. No change needed to getScoreAnchor() return — score position is unchanged.
7. Add this.addChild(this.timerLabel) in constructor after existing labels.
  </action>
  <acceptance_criteria>
- toBoardPosition gridX=113.5, gridY=120, layerOffset=28 — verify with grep for these numeric constants in GameScreen.ts
- HUD constructor accepts onBack?: () => void parameter
- Back button Container has on('pointertap', ...) that invokes this.onBack?.()
- HUD has timerLabel Text at x=designWidth*0.615, fontSize=30, fill=0xfff4d6
- comboLabel.x moved from 0.73 to 0.78 * designWidth
- hud.update() accepts optional 4th parameter timerMs?: number
- GameScreen updates HUD with elapsed time in ticker loop
- npm run build succeeds
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Tiles render edge-to-edge on same layer. Upper layers show classic half-cover stacking. HUD back button fires callback per D-11. Timer MM:SS displays between score and combo.</done>
</task>

<task type="auto">
  <name>Task 3: App.ts level tracking, settings wiring, and feature verification</name>
  <files>src/app/App.ts, src/renderer/screens/ResultScreen.ts</files>
  <read_first>
Read these files before editing:
- src/app/App.ts — createApp(), startLevel(), screen event wiring, screenManager.show() calls
- src/renderer/screens/HomeScreen.ts — START_LEVEL and OPEN_SETTINGS event names from Task 1
- src/renderer/screens/ResultScreen.ts — setStats(), ResultStats interface, NEXT_LEVEL event, next level button label
- src/renderer/screens/GameScreen.ts — static WIN event name, GameStats shape, GameScreenOptions
- src/app/config.ts — colors (gameBg, homeBg for screen backgrounds)
  </read_first>
  <action>
App.ts changes per D-01, D-02, D-11, D-14:
1. Add let currentLevel = 1 at module scope (above createApp function).
2. Add helper function updateHomeLevelLabel() that calls homeScreen.setLevel(currentLevel). This must be defined after homeScreen is created, so define it inside createApp or as a local function that captures homeScreen.
3. Keep startLevel(level: number) signature — call await gameScreen.startLevel(level) then await screenManager.show('game', { backgroundColor: config.colors.gameBg }).
4. Change home START_LEVEL listener from homeScreen.on(HomeScreen.START_LEVEL, (level) => ...) to homeScreen.on(HomeScreen.START_LEVEL, () => startLevel(currentLevel)).
5. In the WIN handler: after resultScreen.setStats(stats), add currentLevel = stats.nextLevel; and call updateHomeLevelLabel().
6. In the NEXT_LEVEL handler: the handler receives (level) from result screen event. Use startLevel(currentLevel) since WIN already advanced currentLevel. Or keep using the event payload for safety.
7. Wire settings navigation: add homeScreen.on(HomeScreen.OPEN_SETTINGS, () => void screenManager.show('settings', { backgroundColor: config.colors.gameBg }));
8. Wire back button: add onBack?: () => void to the GameScreenOptions interface in GameScreen.ts if not already there. In GameScreen constructor, pass options.onBack to new HUD(options.onBack). In App.ts, when creating GameScreen (in createApp), provide: onBack: () => { void screenManager.show('home', { immediate: true, backgroundColor: config.colors.homeBg }); }. Per D-11: immediate transition, no confirmation, discard progress.
9. At end of createApp, after all event wiring and before final screenManager.show('home', ...), call updateHomeLevelLabel().
10. Platform verification (no code changes): initial screen show already uses screenManager.show('home', { immediate: true, backgroundColor: config.colors.homeBg }) — verify this exists.

ResultScreen.ts (verify existing, no changes expected):
- ResultStats interface already has level, nextLevel, elapsedSeconds, score, combo.
- setStats() already updates all display labels.
- NEXT_LEVEL event already emits via next button onTap.
- Stat labels for time/score/combo, beat ratio, progress text already rendered.
- If any field is missing: add the display element per UI-SPEC layout contract section Result Screen.

Feature verification (confirm-only, no code changes):
- VISL-01: Phase 1 procedural assets (bg_game, tile textures, deco elements) use Oriental style. Build and visually verify.
- VISL-02: Phase 1 already has tile highlight, tile-to-tray flight (easeOutBack, 250ms), match removal. Run grep for animateTileToTray and tileFlightMs in GameScreen.ts to confirm.
- ACCS-01: Tile 227x120 design px exceeds WCAG 24px minimum. Confirm in config.ts tile.width/tile.height.
- ACCS-02: TileFace 0xFAFAF8 on dark background, Noto Sans SC. Run grep for tileFace and fontFamily in TileSprite.ts and config.ts.
- PLAT-01: 1080x2400 design base. Confirm config.ts designWidth=1080, designHeight=2400.
  </action>
  <acceptance_criteria>
- currentLevel variable exists in App.ts, starts at 1
- HomeScreen START_LEVEL listener uses () => startLevel(currentLevel) — no payload
- After WIN, currentLevel advances to stats.nextLevel and updateHomeLevelLabel() is called
- HomeScreen.OPEN_SETTINGS listener navigates to screenManager.show('settings', ...)
- HUD back button (via onBack callback) returns to home with immediate: true, no confirmation (D-11)
- updateHomeLevelLabel() called at startup and after WIN
- ResultScreen shows elapsedSeconds, score, combo, beatRatio, nextLevel — all display elements present
- npm run build succeeds
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Home screen shows dynamic level number. Tapping starts current level. After WIN, level advances. HUD back returns home immediately (D-11). Gear icon opens settings placeholder. All Phase 1 features verified intact per requirements VISL-01, VISL-02, ACCS-01, ACCS-02, RSLT-01, RSLT-02, PLAT-01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A — Phase 2 Plan 01 | No new trust boundaries. All UI rendering is client-side PixiJS. No backend, no auth, no user data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | npm install (no new packages) | low | accept | Zero new npm packages in Phase 2. Existing pixi.js and devDependencies verified in Phase 1. |
</threat_model>

<verification>
Execute tasks sequentially. After each task, verify with npm run build. After Task 3, run npm run dev and manually confirm: home screen shows level button with current level number, tap starts game, tiles render with new grid spacing, HUD shows timer updating, back button returns home immediately, gear icon navigates to a blank settings screen.
</verification>

<success_criteria>
1. npm run build passes with zero TypeScript errors.
2. ScreenManager accepts 'settings' as a ScreenName.
3. Home screen button dynamically displays the current level number.
4. Tiles are positioned with gridX=113.5, gridY=120, layerOffset=28.
5. HUD timer updates during gameplay and stops on WIN/deadlock.
6. HUD back button immediately returns to home (D-11).
7. Completing a level advances currentLevel; home button reflects new number.
8. Gear icon navigates to a settings screen placeholder.
</success_criteria>

<output>
Create .planning/phases/02-ui-ux-accessibility-game-flow/02-01-SUMMARY.md when done
</output>

## Artifacts This Phase Produces

### Types extended
- ScreenName union: 'home' | 'game' | 'result' | 'settings' (from 'home' | 'game' | 'result')
- GameScreenOptions interface: onBack?: () => void (new optional field)

### Classes — new properties and methods
- ScreenManager.settings: Container (readonly, new)
- ScreenManager.screens record: settings entry added
- ScreenManager.transitionScale record: settings: 1 entry added
- HomeScreen.levelButton: Button (private, was local variable)
- HomeScreen.OPEN_SETTINGS = 'open-settings' (static readonly, new)
- HomeScreen.setLevel(n: number): void (public, new)
- HUD.timerLabel: Text (private, new)
- HUD.onBack?: () => void (private, new constructor parameter)
- HUD.update(level, score, combo, timerMs?): void (extended signature)
- GameScreen.startedAt: number (private, new)
- GameScreen.toBoardPosition() internals: gridX=113.5, gridY=120, layerOffset=28 (changed)
- GameState.isTerminal(): boolean (public, new — if terminal field was private)

### Module-level state added
- App.ts: currentLevel: number (let, starts at 1)
- App.ts: updateHomeLevelLabel(): void (helper function, captures homeScreen)
