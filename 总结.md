# Vita Mahjong 工程总结

> 本工程依据《乐信圣文 AI 方向岗位测试题》完成，复刻 Vita Mahjong 核心游戏流程：首页 → 游戏页 → 结果页，支持手机设备游玩至少 20 关。

---

## 一、项目概述

### 1.1 交付物

- 可运行的移动端 H5 游戏工程（TypeScript + Vite + PixiJS v8）
- 20 关可玩关卡数据（`public/levels/1.json` ~ `20.json`）
- 程序化生成的纹理资源（`public/assets/`）
- 单元测试覆盖核心逻辑（Vitest，58 个测试全部通过）
- PWA 配置，支持离线缓存与独立运行

### 1.2 核心体验

- 首页：国风木质格栅背景、Logo、飘落竹叶、当前关卡入口
- 游戏页：深绿色绒布桌面、多层立体牌面、选牌飞入托盘、匹配粒子爆炸、连击反馈
- 结果页：金色莲花、祥云装饰、时间 / 分数 / 连击统计、击败玩家比例、下一关入口

---

## 二、实现思路

### 2.1 技术栈选型

| 层级 | 技术 | 说明 |
|------|------|------|
| 渲染引擎 | PixiJS v8 | GPU 加速 2D 渲染，适合大量牌面节点与粒子动效 |
| 构建工具 | Vite | 快速开发服务器、PWA 插件、TypeScript 原生支持 |
| 类型 / 脚本 | TypeScript + tsx | 全类型安全，关卡生成工具直接运行 TS |
| 测试 | Vitest | 核心逻辑单元测试，无头环境即可运行 |
| 部署形态 | H5 / PWA | 手机浏览器打开即可玩，可添加到桌面 |

### 2.2 整体架构

采用 **Model（数据）+ Renderer（表现）+ App（流程编排）** 三层结构：

```
src/
├── app/          # 启动、配置、资源加载、持久化、性能检测
├── engine/       # 核心玩法：棋盘、托盘、自由牌判定、可解性验证
├── generator/    # 关卡布局生成 + 可解发牌
├── renderer/     # PixiJS 屏幕、组件、动效、粒子
├── audio/        # Web Audio API 程序化音效与背景音乐
└── __tests__/    # 单元测试
```

### 2.3 核心玩法实现

#### 自由牌判定

规则要求：牌必须**不被上层覆盖**，且**左侧或右侧至少有一边无紧邻牌**。

实现：
- 每局开始时通过 `buildNeighbors()` 为每张牌建立 `top` / `left` / `right` 邻接表；
- `isBlocked()` 封装判定：`top` 有未拾取牌 或（`left` 与 `right` 同时有未拾取牌）即被锁住。

#### 选牌与匹配

- 点击自由牌后，牌从棋盘位置**飞入托盘**最左侧空 slot（250ms easeOutBack 缓动）；
- 托盘容量固定 **4 个 slot**；
- 若第二张相同牌进入托盘，立即配对成功，触发：
  - 白色方块粒子爆炸；
  - `Good` / `Combo xN` 文字反馈；
  - 分数飘字 `+xxx`；
  - 音效反馈。

#### 计分与连击

```ts
单次匹配得分 = 100 × (1 + min(连击数 - 1, 9) × 0.2)
```

- 连击窗口 3 秒，连续匹配可叠加 Combo；
- 失败条件：托盘填满 4 张不同牌，或场上无自由牌（死锁）。

### 2.4 关卡生成与可解性保证

测试题要求 20 关且每关必须能通关。实现分为三步：

1. **布局生成**（`LayoutBuilder`）：按关卡规格生成多层堆叠坐标，控制列数、行数、层数递增；
2. **成对发牌**（`SolvableDealer`）：基于 seeded RNG 洗牌后，按“逆序消除”策略放置牌面，确保存在一条可行解路径；
3. **可解性验证**（`Solver.isSolvable`）：DFS + 状态记忆，在 4-slot 托盘限制下验证关卡可清空。

生成阶段会断言：
- tileCount 与层数符合规格；
- 每种牌数量为偶数；
- `Solver.isSolvable` 返回 true。

### 2.5 移动端适配

- 设计基准：1080 × 2400（20:9 竖屏）；
- `index.html` 使用固定 `9/20` 比例容器，通过 CSS `min()` 在不同手机上保持比例；
- PixiJS 内部统一使用 designWidth/designHeight 坐标，缩放由容器承担；
- 触控区域在牌面四周扩展 12px，减少误触。

### 2.6 性能优化

- **对象池**：`TilePool` 复用 `TileSprite` 节点，避免频繁创建/销毁；
- **低端机降级**：通过 `isLowEndDevice()` 检测，粒子数量从 72 降至 32；
- **zIndex 分层**：`zIndex = z * 10000 + y * 100 + x`，确保上层牌正确遮挡下层牌；
- **输入锁**：动画期间锁定点击，防止连点造成状态异常。

### 2.7 程序化美术与音效

- 为避免外部美术资源缺失风险，背景、按钮、Logo、莲花、祥云等均使用 PixiJS `Graphics` 程序化绘制；
- 音效与背景音乐使用 Web Audio API 实时合成（五声音阶 BGM + 点击/匹配/胜利/失败音效），不依赖外部音频文件。

### 2.8 体验增强（超出测试题基础要求）

测试题允许“如果你有更好的体验或想法，可以在项目中实现”，因此额外增加了：
- **辅助栏**：Undo（3 次）、Hint（3 次）、Shuffle（1 次），降低后期关卡挫败感；
- **设置页**：音乐 / 音效开关、进度重置；
- **持久化**：localStorage 保存当前关卡与音量设置；
- **PWA**：`vite-plugin-pwa` 配置，支持离线游玩与桌面图标。

---

## 三、遇到的问题与解决方案

### 3.1 自动生成的多层布局可能出现死局

**问题**：随机堆叠的多层牌面很容易进入“无牌可点”或“托盘塞满无法继续”的状态，无法保证可通关。

**解决方案**：
- 引入 `SolvableDealer`：模拟一次理想消除过程，把每一对牌按消除顺序倒序放置到布局中，从而构造出必然存在解的关卡；
- 再用 `Solver.isSolvable` 做二次验证，DFS 搜索时记录已失败状态避免重复探索，并限制最大访问状态数 250,000；
- 生成脚本在构建前自动运行，任一关卡不通过即构建失败。

### 3.2 自由牌判定与层级遮挡的边界情况

**问题**：直接用坐标重叠判断遮挡容易出错，例如“仅边缘接触”不应视为覆盖，而左右邻居必须精确到 `x ± 2` 的网格关系。

**解决方案**：
- 牌面尺寸统一为 2×1 的逻辑网格（宽 2、高 1）；
- `overlaps()` 使用 AABB 检测上层牌是否真正覆盖；
- `buildNeighbors()` 仅当 `other.z === s.z` 且 `other.y === s.y` 时才检查左右邻居，避免斜向误连。

### 3.3 选牌飞入托盘的世界坐标转换

**问题**：棋盘会根据关卡大小动态缩放/居中，牌的世界坐标与托盘屏幕坐标不在同一坐标系，直接插值会导致飞行动画偏移。

**解决方案**：
- 在 `GameScreen.animateTileToTray` 中，先把托盘 slot 的屏幕坐标转换为 `boardLayer` 局部坐标；
- 在局部坐标系内做 easeOutBack 插值；
- 动画期间临时提升该牌 `zIndex` 到 999,999，确保飞行过程中不会被其他牌遮挡。

### 3.4 移动端浏览器自动播放限制

**问题**：Web Audio 在页面加载后不能自动播放，必须等待用户首次交互。

**解决方案**：
- `AudioManager.init()` 延迟到首次点击“关卡”按钮时调用；
- 初始化时若 `AudioContext.state === 'suspended'`，主动 `resume()`；
- 所有 `playSfx` / `startBgm` 都兼容 `ctx.state !== 'running'` 的情况，自动恢复或静默跳过。

### 3.5 大棋盘在不同手机上可能超出屏幕

**问题**：第 20 关 128 张牌，若按固定尺寸渲染会超出屏幕边界。

**解决方案**：
- `renderBoard()` 计算棋盘包围盒后，动态计算 `boardScale = min(1, 0.96 * designWidth / width, 0.6 * designHeight / height)`；
- 自动缩放并居中，保证从第 1 关到第 20 关都能完整显示在棋盘区域内。

### 3.6 测试环境的 Node.js 与 DOM 差异

**问题**：PixiJS 渲染逻辑依赖浏览器 DOM 和 Canvas，难以在 Node 单元测试中直接覆盖。

**解决方案**：
- 核心逻辑（`BoardModel`、`TrayModel`、`GameState`、`Solver`、`validateLevel`）保持纯 TypeScript，不依赖 PixiJS；
- 渲染层测试使用 Vitest 的 `happy-dom`/`jsdom` 环境结合轻量 mock，验证 ScreenManager 状态切换；
- 生成器与可解性测试直接在 Node 环境运行。

---

## 四、测试与验证

运行命令：

```bash
npm test
```

结果：

```
Test Files  11 passed (11)
     Tests  58 passed (58)
  Duration  1.13s
```

覆盖范围：
- 棋盘模型（拾取、自由牌、洗牌、死锁检测）
- 托盘模型（匹配、满槽、恢复）
- 游戏状态（计分、连击、胜负判定、Undo/Hint/Shuffle）
- 可解性求解器
- 关卡生成与验证
- 自由牌判定边界
- 持久化
- 渲染节点数量预算

---

## 五、运行方式

### 开发调试

```bash
npm install
npm run dev
```

### 构建生产包

```bash
npm run build
```

构建产物位于 `dist/`，可直接部署到任意静态服务器或 Vercel。

### 手机体验

- 同一局域网内通过 `npm run dev -- --host` 暴露地址；
- 手机浏览器访问本地 IP:5173；
- 或部署后扫描二维码访问。

---

## 六、后续可优化方向

1. **关卡编辑器**：当前布局由 `LayoutBuilder` 程序化生成，后续可可视化调整每关形状；
2. **星级评定**：当前计分系统已有基础，可补充“时间目标 / 连击目标”三星判定；
3. **动画丰富度**：增加结算页数据逐栏展示、卡牌拾取时的弹性形变；
4. **手势适配**：针对大屏手机优化底部辅助栏位置，避免与系统手势冲突；
5. **多语言**：当前为中文，可扩展 i18n；
6. **社交分享**：结果页增加“分享成绩图”功能。

---

## 七、提交物清单

| 文件/目录 | 说明 |
|-----------|------|
| `src/` | 完整源码（渲染、逻辑、生成器、音效） |
| `public/levels/` | 20 关关卡 JSON |
| `public/assets/` | 程序化纹理与字体 |
| `package.json` / `vite.config.ts` | 依赖与构建配置 |
| `PRD.md` | 需求与设计依据 |
| `工程总结.md` | 本总结文档 |
| `__tests__/` | 单元测试 |

---

*文档生成时间：2026-07-05*
