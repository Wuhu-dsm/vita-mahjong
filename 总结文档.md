# Vita Mahjong 工程总结

> 基于《乐信圣文 AI 方向岗位测试题》完成的移动端麻将连连看复刻项目。全文按真实开发过程记录：先写实现思路，再写实际踩到的坑和对应的解决方案。

---

## 一、项目概述

交付物：

- 可运行的移动端 H5 游戏（TypeScript + Vite + PixiJS v8）
- 20 关可玩关卡（`public/levels/1.json` ~ `20.json`）
- 程序化生成的纹理资源（`public/assets/`）
- 核心逻辑单元测试 58 个，全部通过
- PWA 配置，支持离线缓存

核心流程：首页 → 游戏页 → 结果页，支持选牌、匹配、消除、撤销/提示/洗牌、连击计分、通关/失败结算。

---

## 二、实现思路

### 2.1 技术选型

- **PixiJS v8**：2D 渲染用 GPU 加速，牌面节点多、粒子动效多，PixiJS 比纯 DOM 或 Canvas 2D 更合适。
- **Vite**：开发服务器快，PWA 插件成熟，TypeScript 原生支持。
- **TypeScript**：全类型安全，关卡生成器直接跑 TS 脚本，不用额外转译。
- **Vitest**：核心逻辑单元测试，浏览器渲染层用 happy-dom/jsdom 轻量 mock。

### 2.2 用 GSD 分阶段推进

整个项目用 GSD（Goal-Space-Direction）工作流拆成 3 个 Phase 执行，每个 Phase 先有 CONTEXT / RESEARCH / DISCUSSION，再出 PLAN，执行后做 UAT 和 VERIFICATION。

| Phase | 目标 | 结果 |
|-------|------|------|
| Phase 1：Core Engine & Solvable Levels | 棋盘模型、自由牌判定、可解关卡生成、基础渲染 | 完成，5/5 plans |
| Phase 2：UI/UX, Accessibility & Game Flow | 完整三屏流程、音频、辅助按钮、UI 还原 | 完成，3/3 plans |
| Phase 3：Polish, Persistence & Distribution | 持久化、粒子、性能分级、PWA | 完成，2/2 plans |

每个 Phase 的决策、截图依据、失败用例都落在 `.planning/phases/` 里，不是拍脑袋写的。

### 2.3 核心引擎

采用 **Model + Renderer + App** 三层：

```
src/
├── app/          # 启动、配置、资源加载、持久化
├── engine/       # BoardModel、TrayModel、GameState、Solver
├── generator/    # 关卡布局 + 可解发牌
├── renderer/     # PixiJS 屏幕、组件、动效
├── audio/        # Web Audio API 音效与 BGM
└── __tests__/    # 单元测试
```

#### 自由牌判定

规则：牌必须不被上层覆盖，且左右至少一侧无紧邻牌。

实现：

- 开局 `buildNeighbors()` 为每张牌建立 `top` / `left` / `right` 邻接表；
- `isBlocked()` 判定：`top` 有未拾取牌，或左右同时被夹住，即被锁住。

#### 选牌与匹配

- 点击自由牌后，牌从棋盘位置飞入托盘最左侧空 slot（easeOutBack，约 250ms）；
- 托盘容量 4 slot；
- 两张相同牌进入托盘后立即配对，触发粒子爆炸、`Good` / `Combo` 文字、分数飘字、音效。

#### 计分

```ts
单次匹配得分 = 100 × (1 + min(连击数 - 1, 9) × 0.2)
```

连击窗口 3 秒。失败条件：托盘填满 4 张不同牌，或场上无自由牌（死锁）。

### 2.4 关卡生成：必须保证可解

测试题要求 20 关且每关可通关。实现分三步：

1. **布局生成**（`LayoutBuilder`）：按关卡规格生成多层堆叠坐标，第 20 关最多 128 张、7 层；
2. **成对发牌**（`SolvableDealer`）：用 seeded RNG 洗牌后，按“逆序消除”策略放置牌面，确保存在一条可行解；
3. **可解性验证**（`Solver.isSolvable`）：DFS + 状态记忆，在 4-slot 托盘限制下验证关卡可清空。

生成脚本在构建前自动运行，任一关卡不通过即构建失败。

### 2.5 UI 还原：Codex 负责视觉还原，人工兜底

PRD 提供了 12 张关键节点截图（`assets/screenshots/key_nodes/`）和录屏逐帧素材。Phase 2 的 UI 还原主要策略是：

- 用 Codex 根据截图生成首批视觉代码（TileSprite、HomeScreen、GameScreen、ResultScreen 等）；
- 人工对照截图逐屏验收，修正颜色、比例、动效细节；
- 所有纹理资源程序化生成，不依赖外部美术文件，避免素材缺失风险。

实际还原过程中发现 Codex 生成的初版存在几类问题：

- 牌面圆角、侧边厚度、阴影方向与截图不一致；
- 首页 Logo 比例和“發”字标识位置偏差；
- 游戏页 HUD 字段与测试题截图不一致（录屏用 IQ，测试题用“分数/匹配”）。

这些问题都在 Phase 2 执行和后续 quick task 里逐条修正。

### 2.6 移动端适配

- 设计基准 1080×2400（20:9 竖屏）；
- `index.html` 用固定 `9/20` 比例容器，通过 CSS `min()` 在不同手机上保持比例；
- PixiJS 内部统一用 designWidth/designHeight 坐标，缩放由外层容器承担；
- 触控区域在牌面四周扩展 12px，减少误触。

### 2.7 程序化美术与音效

- 背景、按钮、Logo、莲花、祥云等全部用 PixiJS `Graphics` 程序化绘制；
- 音效与 BGM 用 Web Audio API 实时合成（五声音阶 BGM + 点击/匹配/胜利/失败音效），不依赖外部音频文件。

---

## 三、遇到的问题与解决方案

### 3.1 自由牌判定与层级遮挡的边界问题

**问题**：早期用坐标重叠判断遮挡时，边缘接触被误判为覆盖，左右邻居的网格关系也不精确，导致有些牌该锁没锁、该解锁没解锁。

**解决**：

- 牌面统一用 2×1 的逻辑网格（宽 2、高 1）；
- `overlaps()` 用 AABB 检测上层牌是否真正覆盖；
- `buildNeighbors()` 仅当同层同高时才检查左右邻居，避免斜向误连。

### 3.2 随机布局容易出现死局

**问题**：直接随机堆叠多层牌面，很容易出现“无牌可点”或“托盘塞满无法继续”的状态，无法保证可通关。

**解决**：

- 引入 `SolvableDealer`：模拟一次理想消除过程，把每一对牌按消除顺序倒序放置到布局中，构造出必然存在解的关卡；
- 再用 `Solver.isSolvable` 做二次验证，DFS 搜索时记录已失败状态避免重复探索，最大访问状态数限制 250,000；
- 生成脚本在构建前自动运行，任一关卡不通过即构建失败。

### 3.3 UI 还原细节对不上截图

**问题**：Codex 根据截图生成的首版视觉代码在几个关键点上与 PRD 截图有偏差：

- 牌面视觉：圆角过大、绿色侧边厚度不对、阴影方向偏软；
- 首页：Logo 下方“發”字标识位置和样式不对；
- 游戏页 HUD：顶部显示的是 IQ/时间，但测试题截图要求“关卡 + 分数 + 匹配”。

**解决**：

- 人工对照 `key_nodes/` 截图逐屏验收；
- 重新调整 `TileSprite` 的牌面、侧边、阴影、高光参数；
- 把 HUD 改成测试题截图的“关卡 / 分数 / 匹配”三字段布局；
- 后续 quick task `260705-cif` 进一步修复首页和游戏页视觉缺陷（如增加落叶动画、移除多余按钮、调整托盘消除光效等）。

### 3.4 阻挡提示箭头残留并遮挡周围牌

**问题**：Phase 1 UAT 第 3 项未通过。点击被锁住的牌后，牌面变暗且红色箭头一直不消失，箭头还会伸到相邻牌的位置，遮挡周围牌。

**排查**：

- `GameScreen` 在阻挡点击时调用 `tile.setBlocked(true)` 和 `blockedHint.showAt(...)` 后没有定时恢复；
- `TileSprite.setBlocked(true)` 会在牌两侧显示 54px 宽的箭头，超出牌面边界；
- `BlockedHint` 本身也有另一组临时箭头，两套箭头叠加导致视觉混乱。

**解决**：

- 增加 `BlockedFeedbackLifecycle`，1.2 秒后自动清除 `TileSprite` 的阻挡状态；
- 移除牌面两侧的持久箭头，只保留 `BlockedHint` 的临时提示；
- 限制提示箭头范围，不再伸入相邻牌区域。

对应提交：`1cba8d9 fix(01-05): remove persistent blocked tile arrows`。

### 3.5 选牌飞入托盘的世界坐标转换错误

**问题**：棋盘会根据关卡大小动态缩放/居中，牌的世界坐标与托盘屏幕坐标不在同一坐标系，直接插值会导致飞行动画偏移。

**解决**：

- 在 `GameScreen.animateTileToTray` 中，先把托盘 slot 的屏幕坐标转换为 `boardLayer` 局部坐标；
- 在局部坐标系内做 easeOutBack 插值；
- 动画期间临时提升该牌 `zIndex` 到 999,999，确保飞行过程中不会被其他牌遮挡。

### 3.6 快速连点被丢弃

**问题**：Phase 1 动画期间全局锁死输入，玩家快速连续点牌时，后面的点击会被直接丢弃，手感很卡。

**解决**：

- Quick task `260705-d70` 重构 `GameScreen` 的输入处理：
  - 移除全局 `inputLocked` 丢弃逻辑；
  - 每次点击立即处理，用 `activeAnimations` 和 `flyingStones` 跟踪正在飞的牌；
  - 棋盘重绘延迟到所有动画结束后自动触发；
  - 辅助按钮在动画期间禁用，避免状态冲突。

对应提交：`7104bad fix(gameplay): execute rapid taps directly while deferring board re-render`。

### 3.7 关卡布局太规则，像完美矩形

**问题**：生成的关卡每层都是完整矩形，视觉上单调，缺少传统 Mahjong Solitaire 的不规则堆叠感。

**解决**：

- 在 `LayoutBuilder.layerPositions` 中加入 `distributeRowWidths`，按 seeded hash 对每行宽度做确定性扰动；
- 当某层接近完美矩形时，额外增加一行并做锯齿状边缘，同时保持总牌数不变；
- 重新运行 `npm run generate-levels` 生成 20 关，并用 `Solver.isSolvable` 验证全部可解。

### 3.8 撤销操作后托盘状态不一致

**问题**：Phase 2 UAT 发现：

- 单点一张牌进托盘后点撤销，无响应；
- 消一对后撤销，对牌复原但托盘里还残留之前的牌。

**解决**：

- 修正 `GameState.undo()` 逻辑：撤销时同时恢复 `BoardModel` 和 `TrayModel` 的状态；
- 撤销后刷新托盘显示，移除不应存在的牌；
- 增加测试覆盖单牌撤销和配对撤销两种情况。

### 3.9 移动端浏览器自动播放限制

**问题**：Web Audio 在页面加载后不能自动播放，必须等待用户首次交互。

**解决**：

- `AudioManager.init()` 延迟到首次点击“关卡”按钮时调用；
- 初始化时若 `AudioContext.state === 'suspended'`，主动 `resume()`；
- 所有 `playSfx` / `startBgm` 都兼容 `ctx.state !== 'running'` 的情况，自动恢复或静默跳过。

### 3.10 大棋盘在不同手机上可能超出屏幕

**问题**：第 20 关 128 张牌，若按固定尺寸渲染会超出屏幕边界。

**解决**：

- `renderBoard()` 计算棋盘包围盒后，动态计算 `boardScale = min(1, 0.96 * designWidth / width, 0.6 * designHeight / height)`；
- 自动缩放并居中，保证从第 1 关到第 20 关都能完整显示在棋盘区域内。

### 3.11 TypeScript 独立类型检查失败

**问题**：`tsc --noEmit` 和 `tsc -b --noEmit` 会因为项目引用和 Vite 测试配置的既有类型问题报错，但 `npm test` 和 `npm run build` 正常。

**处理**：

- 这个问题未阻塞交付，记录为 deferred item；
- 验证以 `npm test` 和 `npm run build` 通过为准。

### 3.12 测试环境的 Node.js 与 DOM 差异

**问题**：PixiJS 渲染逻辑依赖浏览器 DOM 和 Canvas，难以在 Node 单元测试中直接覆盖。

**解决**：

- 核心逻辑（`BoardModel`、`TrayModel`、`GameState`、`Solver`、`validateLevel`）保持纯 TypeScript，不依赖 PixiJS；
- 渲染层测试使用 Vitest 的 `happy-dom`/`jsdom` 环境结合轻量 mock，验证 `ScreenManager` 状态切换；
- 生成器与可解性测试直接在 Node 环境运行。

---

## 四、测试与验证

运行命令：

```bash
npm test
```

结果：

```
Test Files  11 passed (11)
     Tests  58 passed (58)
  Duration  1.13s
```

覆盖范围：

- 棋盘模型（拾取、自由牌、洗牌、死锁检测）
- 托盘模型（匹配、满槽、恢复）
- 游戏状态（计分、连击、胜负判定、Undo/Hint/Shuffle）
- 可解性求解器
- 关卡生成与验证
- 自由牌判定边界
- 持久化
- 渲染节点数量预算

---

## 五、运行方式

### 开发调试

```bash
npm install
npm run dev
```

### 构建生产包

```bash
npm run build
```

构建产物位于 `dist/`，可直接部署到任意静态服务器或 Vercel。

### 手机体验

- 同一局域网内通过 `npm run dev -- --host` 暴露地址；
- 手机浏览器访问本地 IP:5173；
- 或部署后扫描二维码访问。

---

## 六、后续可优化方向

1. **关卡编辑器**：当前布局由 `LayoutBuilder` 程序化生成，后续可可视化调整每关形状；
2. **星级评定**：当前计分系统已有基础，可补充“时间目标 / 连击目标”三星判定；
3. **动画丰富度**：增加结算页数据逐栏展示、卡牌拾取时的弹性形变；
4. **手势适配**：针对大屏手机优化底部辅助栏位置，避免与系统手势冲突；
5. **多语言**：当前为中文，可扩展 i18n；
6. **社交分享**：结果页增加“分享成绩图”功能。

---

## 七、提交物清单

| 文件/目录 | 说明 |
|-----------|------|
| `src/` | 完整源码（渲染、逻辑、生成器、音效） |
| `public/levels/` | 20 关关卡 JSON |
| `public/assets/` | 程序化纹理与字体 |
| `package.json` / `vite.config.ts` | 依赖与构建配置 |
| `PRD.md` | 需求与设计依据 |
| `总结.md` | 本总结文档 |
| `__tests__/` | 单元测试 |

---

*文档更新时间：2026-07-05*
