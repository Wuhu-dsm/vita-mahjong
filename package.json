{
  "name": "vita-mahjong",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "predev": "npm run generate-assets && npm run generate-levels",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "prebuild": "npm run generate-assets && npm run generate-levels",
    "generate-assets": "tsx tools/generate-assets.ts",
    "generate-levels": "tsx tools/generate-levels.ts",
    "pretest": "npm run generate-assets && npm run generate-levels",
    "test": "vitest"
  },
  "dependencies": {
    "pixi.js": "^8.19.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^6.0.3",
    "vite": "^8.1.3",
    "vite-plugin-pwa": "^1.3.0",
    "vitest": "^4.1.9"
  }
}
